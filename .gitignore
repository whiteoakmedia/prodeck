# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Rust build output (huge) and wrangler caches
src-tauri/target
.wrangler

# Never commit credentials
*.key
*.pem
.dev.vars
.env
ga4-key.json

# Per-site local config (your identifiers, domain, update feed) — never committed
src-tauri/tauri.local.conf.json
# Derived from it at booth-install time (same private identifiers/feed).
src-tauri/tauri.booth-install.conf.json
crew-edge/wrangler.local.jsonc

# publish-update.sh output (large DMG + feed)
update-server/

# scripts/build-dmg.sh output
/ProDeck.dmg

# scripts/release-public.sh outputs
release/
